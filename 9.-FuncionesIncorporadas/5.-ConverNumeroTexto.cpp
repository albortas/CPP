#include<iostream>
using namespace std;

int main(){

    // Convertir numero a cadena, to_string()
    int a = 10, b = 20;
    cout << a + b << endl;

    //Convertimos los valores enteros a tipo_cadena
    cout << to_string(a) + to_string(b) << endl;
    
    return 0;

}